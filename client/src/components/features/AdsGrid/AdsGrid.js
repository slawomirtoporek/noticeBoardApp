import { Row, Col } from "react-bootstrap";
import AdWievShort from '../AdViewShort/AdViewShort';

const AdsGrid = ({ ads }) => {
  
  return (
    <></>
    // <Row>
    //   {
    //     ads.map((ad, index) => (
    //       <Col key={index} lg={4}  md={6} xs={12} >
    //         <AdWievShort id={ad._id} title={ad.title} image={ad.image} location={ad.location} />
    //       </Col>
    //     ))
    //   }
    // </Row>
  );
};

export default AdsGrid;